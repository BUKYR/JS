function pow(x, n) {
    return Math.pow(x, n)
}

let x = +prompt("Введите X", '');
let n = +prompt("Введите степень", '');

if (n > 0 && (n % 2 == 0)) {
    alert(`Число в степени = ` +  pow(x,n))
} else {
    `Степень ${n} не поддерживается, используйте натуральное число`
}